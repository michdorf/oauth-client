{
  "name": "oauth-client",
  "version": "1.1.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "rimraf dist && tsc && rollup -c rollup.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@open-wc/building-rollup": "^1.10.0",
    "deepmerge": "^4.3.1",
    "rimraf": "^3.0.2",
    "rollup": "^2.79.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "base64-arraybuffer": "^1.0.2",
    "moduli": "github:michdorf/moduli"
  }
}
